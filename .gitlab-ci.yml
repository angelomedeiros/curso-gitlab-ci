image: node

# Stages default quando não indicados: build, test e deploy

stages:
  - build
  - test
  - deploy

construir site:
  script:
    # - ls -ltra
    # - pwd
    # - whoami
    - cd website
    - npm install
    - npm run build