image: node

# Stages default quando não indicados: build, test e deploy

stages:
  - build
  - test
  - deploy

construir site:
  stage: build
  artifacts:
    paths:
      - ./website/build/curso-gitlab-ci
  script:
    # - ls -ltra
    # - pwd
    # - whoami
    - cd website
    - npm install
    - npm run build